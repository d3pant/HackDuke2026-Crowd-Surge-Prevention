# Set to false when backend WebSocket is available
VITE_MOCK_WS=true
# No FastAPI on :8000 — skip /api/incidents and /api/guards (stops Vite proxy ECONNREFUSED noise)
VITE_SKIP_BACKEND_API=true
# Mock: cycle speed through src/fixtures/scenes/scene-*.json (ms)
VITE_MOCK_SCENE_MS=1600
